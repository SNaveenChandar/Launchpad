_schema-version: "3.2"
ID: comchslaunchpad
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: comchslaunchpad-destination-content
  type: com.sap.application.content
  requires:
  - name: comchslaunchpad-destination-service
    parameters:
      content-target: true
  - name: comchslaunchpad-repo-host
    parameters:
      service-key:
        name: comchslaunchpad-repo-host-key
  - name: comchslaunchpad-uaa
    parameters:
      service-key:
        name: comchslaunchpad-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: comchslaunchpad_html_repo_host
          ServiceInstanceName: comchslaunchpad-html5-srv
          ServiceKeyName: comchslaunchpad-repo-host-key
          sap.cloud.service: comchslaunchpad
        - Authentication: OAuth2UserTokenExchange
          Name: comchslaunchpad_uaa
          ServiceInstanceName: comchslaunchpad-xsuaa-srv
          ServiceKeyName: comchslaunchpad-uaa-key
          sap.cloud.service: comchslaunchpad
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: comchslaunchpad-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comchslaunchpad-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comchslaunchpad.zip
      name: comchslaunchpad
      target-path: resources/
- name: comchslaunchpad
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: comchslaunchpad-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: comchslaunchpad-destination-service
    service-plan: lite
- name: comchslaunchpad-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: comchslaunchpad-xsuaa-srv
    service-plan: application
- name: comchslaunchpad-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: comchslaunchpad-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
